/secretsmanager
/.secrets.json
/.secrets.json.tmp
/cmd/secretsmanager/secretsmanager
/go-fsck.json

# splint writes its document beside the tree it describes, and a job that
# fails leaves it behind. go-fsck reads it and writes its own.
splint.json
